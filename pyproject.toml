[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ncaam-sim"
version = "0.1.0"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
